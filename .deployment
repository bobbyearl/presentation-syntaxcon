[config]
command = npm install && npm run build && "$KUDU_SYNC_CMD" -v 500 -f "$DEPLOYMENT_SOURCE/$STACHE_BUILD_DIRECTORY" -t "$DEPLOYMENT_TARGET" -n "$NEXT_MANIFEST_PATH" -p "$PREVIOUS_MANIFEST_PATH" -i ".git;.hg;.deployment;deploy.sh"